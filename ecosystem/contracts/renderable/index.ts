/**
 * A value that the rendering engine can display.
 *
 * Covers virtual DOM elements, primitive values, null, and undefined.
 * This is the standard type for anything that can appear in a rendered
 * tree — component output, children props, signal values destined for
 * display.
 */
export type {ComponentChild as Renderable} from 'preact';
