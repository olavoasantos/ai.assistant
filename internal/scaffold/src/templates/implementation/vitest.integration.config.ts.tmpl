import {mergeConfig} from 'vitest/config';
import viteConfig from './vite.config';
import integrationConfig from '@local.pkg/config/testing/integration';

export default mergeConfig(viteConfig, integrationConfig);
